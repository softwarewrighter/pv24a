Implemented 8 memory opcode handlers: loadg/storeg (global word access by offset), addrg/addrl (push address of global/local), load/store (indirect word access), loadb/storeb (indirect byte access). Wired all into dispatch table. Test bytecode stores 'A' to global, loads it back via loadg, then overwrites via addrg+store indirect, loads via addrg+load indirect to get 'B'. Output: AB\n. Expanded globals segment to 8 words.