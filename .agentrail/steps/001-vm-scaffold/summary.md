Created pvm.s scaffold: COR24 entry point, vm_state struct (9 fields), UART helpers (putc/puts using jal calling convention), fetch-decode-execute loop with lbu zero-extend opcode fetch, bounds check (clu), multiply-by-3 dispatch table lookup, 97-entry dispatch table (0x00-0x60), halt handler, invalid opcode trap, all other opcodes stubbed. Memory segment labels defined (code/globals/call-stack/eval-stack/heap). Test bytecode is single halt byte. Assembles to 581 bytes, runs clean: prints 'PVM OK', dispatches halt, prints 'HALT', stops in 248 instructions.